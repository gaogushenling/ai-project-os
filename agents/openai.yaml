interface:
  display_name: "AI Project OS"
  short_description: "Lightweight project memory and task routing."
  default_prompt: "Use $ai-project-os to start from the user-visible outcome and the smallest complete scope, load the necessary context through project routes, and complete the matching acceptance checks before delivery."
